# Package index

## All functions

- [`add_array_coords()`](add_array_coords.md) :

  Add transformed array and pixel coordinates to a `SpatialExperiment`

- [`add_overlap_info()`](add_overlap_info.md) : Add info about how spots
  overlap among capture areas

- [`as.Seurat()`](as.Seurat.md) :

  Convert a `SpatialExperiment` object to a `Seurat` object

- [`build_SpatialExperiment()`](build_SpatialExperiment.md) :

  Build stitched `SpatialExperiment`

- [`merge_overlapping()`](merge_overlapping.md) : Merge overlapping
  spots

- [`prep_fiji_image()`](prep_fiji.md)
  [`prep_fiji_coords()`](prep_fiji.md) : Prepare Fiji outputs for
  building a SpatialExperiment

- [`rescale_fiji_inputs()`](rescale_fiji_inputs.md) : Write same-scale
  hires images for input to Fiji
